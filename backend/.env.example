# Example environment variables for development
# Copy to .env and customize for your environment.

# --------------------
# Server / Networking
# --------------------
NODE_ENV=development
HOST=0.0.0.0

# If PORT is set, it is validated and used (fail-fast if unavailable).
# If PORT is not set, the backend scans PORT_RANGE and locks the first free port.
# PORT=3000
PORT_RANGE=3000-3999

# Comma-separated list for both HTTP CORS + WebSocket CORS.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5174

LOG_LEVEL=info
LOG_PRETTY=true

# --------------------
# Redis (optional)
# --------------------
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# --------------------
# Security (set strong values in production)
# --------------------
# AES_KEY should be a base64 or hex encoded 32-byte key in production
AES_KEY=dev-aes-key-placeholder
SERVER_SECRET=dev-server-secret
SESSION_SECRET=dev-session-secret

# --------------------
# Native tools (optional; platform-specific)
# --------------------
# Leave these unset if you are using bundled toolchains/resources.
# DAP_ADAPTER_PATH=
# GDB_PATH=

# --------------------
# Docker (legacy/optional)
# --------------------
# If you are not using Docker-based workers, leave these unset.
# DOCKER_SOCKET_PATH=
# DOCKER_POOL_SIZE=10
# DOCKER_MAX_POOL_SIZE=20

# --------------------
# Neutralino runtime pack (used for desktop builds)
# --------------------
# In dev, the backend does not require this runtime by default.
# Set to true if you want startup to fail when runtime is missing.
NEUTRALA_RUNTIME_REQUIRED=false

# If true, backend will try to ensure the runtime on startup (even if not required).
# NEUTRALA_RUNTIME_AUTO=true

# Neutralino runtime release version (required for desktop packaging).
# Example: 5.0.0
# NEUTRALA_RUNTIME_VERSION=
#
# Base URL where runtime archives live (optional override).
# Templates supported: {version} {platform} {arch}
# Example (GitHub): https://github.com/neutralinojs/neutralinojs/releases/download/v{version}/
# NEUTRALA_RUNTIME_BASE_URL=
#
# Required checksums (sha256 hex) for each platform archive.
# NEUTRALA_RUNTIME_SHA256_WINDOWS=
# NEUTRALA_RUNTIME_SHA256_MAC=
# NEUTRALA_RUNTIME_SHA256_LINUX=
#
# Supported runtime semver range.
# NEUTRALA_RUNTIME_RANGE=>=1.0.0 <1000.0.0
