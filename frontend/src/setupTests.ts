
// Optional: add @testing-library/jest-dom matchers when available.
// Some environments/sandboxes may not have this dependency installed.
try {
  // eslint-disable-next-line @typescript-eslint/no-require-imports
  require("@testing-library/jest-dom");
} catch {
  // no-op
}
